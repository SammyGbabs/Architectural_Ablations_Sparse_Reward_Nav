# Results Log

**This is an append-only log of experimental results.** Every training run, ablation, and analysis pass gets an entry here. The goal is that at paper-writing time, the results section writes itself from this file.

## Conventions

- Add new entries at the **bottom** of the relevant phase section. Newest results live below older ones, so the file reads chronologically when scrolling top-to-bottom within a phase.
- Every entry has: **date**, **W&B group**, **configuration**, **headline numbers with CIs**, **a one-paragraph interpretation**, and a **link back to W&B**.
- Numbers without confidence intervals are not allowed in this file. If you don't have CIs yet, the entry isn't ready.
- The **interpretation paragraph** is the most important part. It's what becomes the basis for the paper's results section. Write it for your future self, who will be tired and on a deadline.
- If a result later turns out to be wrong (bug in the env, broken seed handling, etc.), **do not delete the entry**. Add a follow-up entry below it explaining what changed and why. Reviewers like to see the history.
- Per-seed raw numbers live in `results/csv/{phase}_{experiment_id}.csv`, not here. This file holds aggregates only.

## Entry template (copy-paste this)

```markdown
### [phase][exp-id] short descriptive title
- **Date:** YYYY-MM-DD
- **Owner:** Samuel | Collaborator
- **W&B group:** `arch-ablations-sparse-reward / {group_name}`
- **W&B URL:** https://wandb.ai/...
- **Config(s):** `configs/{file}.yaml` (paste relevant subset if short)
- **Seeds:** N (list explicitly: 0, 1, 2, ...)
- **Env steps per run:** ###k
- **Headline metric(s):**
  | Metric | Value (IQM) | 95% CI |
  |---|---|---|
  | Return | X.XX | [X.XX, X.XX] |
  | Success rate | X.X% | [X.X%, X.X%] |
  | Collision rate | X.X% | [X.X%, X.X%] |
  | Avg steps | X.X | [X.X, X.X] |
  | Path Optimality Ratio | X.XX | [X.XX, X.XX] |
- **Figure(s):** `figures/{filename}.png` (if any)
- **Generated by:** `Analysis/{script}.py` (the script that produced the numbers + figure)
- **Raw per-seed CSV:** `results/csv/{phase}_{exp_id}.csv`

**Interpretation.** One paragraph. What does this result mean? Does it support or refute which hypothesis? What's the next experiment this implies? Be concrete and don't oversell. If the result is null, say so plainly.

**Caveats / known issues.** Bullets for anything that should make the reader trust the result less. Examples: only 3 seeds (sanity check, not final), one Colab disconnect during run 4 may have lost ~5k steps, hyperparameter X was not tuned for this config.

---
```

---

## Phase 1 — Multi-seed replication of original Tables 1 and 2

**Purpose:** Replicate the rejected paper's Exp 1–5 (DQN) and Exp 1–4 (PPO) configurations at 10 seeds with proper statistics. Determines whether the inverted asymmetry result survives multi-seed reporting (the Phase 1 gate).

**Hypothesis under test:** H1.

**Status:** *(update as runs land)*

### [P1][exp-id] (example — DELETE once first real entry lands)
- **Date:** 2026-XX-XX
- **Owner:** Samuel
- **W&B group:** `arch-ablations-sparse-reward / p1-dqn-exp5-10seeds`
- **W&B URL:** *(paste link)*
- **Config(s):** `configs/p1_dqn_exp5.yaml` — [512, 256] MLP, ε 1.0→0.02 fraction 0.15, lr 3e-4, γ 0.99, buffer 100k, batch 256
- **Seeds:** 10 (0–9)
- **Env steps per run:** 200k
- **Headline metric(s):**
  | Metric | Value (IQM) | 95% CI |
  |---|---|---|
  | Return | — | [—, —] |
  | Success rate | — | [—, —] |
  | Collision rate | — | [—, —] |
  | Avg steps | — | [—, —] |
  | Path Optimality Ratio | — | [—, —] |
  | Wait-action frequency | — | [—, —] |
- **Figure(s):** `figures/p1_dqn_exp5_learning_curve.png`
- **Generated by:** `Analysis/plot_phase1_curves.py`
- **Raw per-seed CSV:** `results/csv/p1_dqn_exp5.csv`

**Interpretation.** *(Fill in once data exists. Example narrative shapes: "Replicates the original 100% SR claim within CI: [97%, 100%]" or "Original SR of 100% was a single-seed artifact: IQM is X% with wide CI suggesting genuine variance.")*

**Caveats / known issues.** *(Fill in.)*

---

## Phase 2 — Capacity-ratio ablation, procedural generalisation, POMDP variants

**Purpose:** Test H1 cleanly by holding total parameter budget constant and varying actor:critic ratio. Test H3 by training on procedural layouts and measuring train-test gap. Resolve the POMDP framing problem from Reviewer 1.

**Hypotheses under test:** H1 (capacity-controlled), H2, H3.

**Status:** *(blocked on Phase 1 gate)*

*(Entries land here.)*

---

## Phase 3 — Cross-environment (MiniGrid), Lipschitz, dynamic, sensor noise

**Purpose:** Test H4 (Lipschitz) by post-hoc analysis of saved checkpoints. Test H5 (cross-env) on MiniGrid FourRooms, MultiRoom-N2-S4, DoorKey-6x6. Address Reviewer 2/3 concerns about static-only env via dynamic-obstacle ablation, and the sim-to-real gap via sensor-noise ablation.

**Hypotheses under test:** H4, H5.

**Status:** *(blocked on Phase 2 completion)*

*(Entries land here.)*

---

## Phase 4 — Hybrid safety architecture, optional GRPO

**Purpose:** Implement PPO planner + DQN safety filter (H6). Optional: GRPO comparison if time allows.

**Hypotheses under test:** H6.

**Status:** *(blocked on Phase 3 completion)*

*(Entries land here.)*

---

## Sanity-check / smoke-test results (not for paper)

This section captures results from runs that were intentionally undersized (3 seeds, short horizons) to verify the pipeline works. **Do not cite these in the paper.** They live here so we don't forget what was tested, but they're not real evidence.

*(Entries land here as smoke tests pass.)*

---

## Cross-references

- **Hypotheses:** see `CLAUDE.md` § Pre-registered hypotheses.
- **Phase plan and gates:** see `docs/rework_proposal.pdf` §6.
- **W&B project:** `arch-ablations-sparse-reward`.
- **Figure manifest:** `figures/README.md`.
- **Per-seed CSVs:** `results/csv/`.
